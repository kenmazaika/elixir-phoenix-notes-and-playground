<users>
  <%= for user <- @users do %>
  <user>
    <id><%= user.id %></id>
    <name><%= user.content %></name>
    <a href="<%= PhoenixCrud.Router.Helpers.user_path(Endpoint, :destroy, user.id) %>" data-method="DELETE" class="rest">Destroy</a> |

    <a href="<%= PhoenixCrud.Router.Helpers.user_path(Endpoint, :edit, user.id) %>">Edit</a>

  </user>
  <br />
  <% end %>
</users>


<br />
<a href="<%= PhoenixCrud.Router.Helpers.user_path(Endpoint, :new) %>" class="btn btn-primary btn-xl">Make a New "User"</a>


<script>
$(function() {
  $(".rest").restfulizer({
  });

  $('form').submit(function(s) {
    var input = $("<input>")
    .attr("type", "hidden")
    .attr("name", "csrf_token").val("<%= @csrf %>");
    $(s.target).append($(input));
  });
});
</script>
